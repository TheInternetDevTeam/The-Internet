/*
Apart from the various Accept: proposals, the transport protocol shouldn't need to worry about the encoding schemes of the data types which it's sending. Still, the discussion is a little broader than HTML, and HTTP must be at least a little sensitive to the expected content (or we would not even have an Accept-language:). Nothing lives in a vacuum.
*/

//TODO: Add HTTP Code Here.